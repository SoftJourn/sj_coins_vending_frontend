import { MachineCellDirective } from './machine-cell.directive';

describe('MachineCellDirective', () => {
  it('should create an instance', () => {
    const directive = new MachineCellDirective();
    expect(directive).toBeTruthy();
  });
});
