import { MachineRowDirective } from './machine-row.directive';

describe('MachineRowDirective', () => {
  it('should create an instance', () => {
    const directive = new MachineRowDirective();
    expect(directive).toBeTruthy();
  });
});
